openapi: 3.0.3
info:
  title: API для работы с данными по переписям (РС/ИВ/ПП)
  version: "1.0.0"
tags:
  - name: FamilyRevision
    description: API для работы с данными по переписям (РС/ИВ/ПП)
paths:
  /family-revision/{id}:
    get:
      tags:
        - FamilyRevision
      operationId: getById
      summary: "Request data by ID"
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
      responses:
        200:
          description: "OK"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FamilyMember"
        404:
          description: "Data not found"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      tags:
        - FamilyRevision
      operationId: delete
      summary: "Delete data by ID"
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: integer
            format: int64
      responses:
        204:
          description: "OK"
        401:
          description: "Unauthorized"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /family-revision:
    post:
      tags:
        - FamilyRevision
      operationId: save
      summary: "Saving data"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FamilyMember"
        required: true
      responses:
        200:
          description: "OK"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FamilyMember"
        400:
          description: "Failed to save data"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        401:
          description: "Unauthorized"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      tags:
        - FamilyRevision
      operationId: update
      summary: "Update only the FamilyRevision entity, not intended for updating other entities (like ArchiveDocument and others)"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FamilyMember"
        required: true
      responses:
        200:
          description: "OK"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FamilyMember"
        400:
          description: "Failed to update data"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        401:
          description: "Unauthorized"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /family-revision/family:
    get:
      tags:
        - FamilyRevision
      operationId: getFamilyRevisionByNum
      summary: "Family revision filter"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FamilyFilter"
        required: true
      responses:
        200:
          description: "OK"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FamilyMemberFullInfo"
        404:
          description: "Data not found"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /family-revision/filter:
    get:
      tags:
        - FamilyRevision
      operationId: filter
      summary: "Search data by filter"
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FamilyMemberFilter"
        required: true
      responses:
        200:
          description: "OK"
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/EasyFamilyMember"
        404:
          description: "Data not found"
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

components:
  schemas:
    FamilyMember:
      $ref: "./model/genealogy/family.revision/family-member.yaml"
    EasyFamilyMember:
      $ref: "./model/genealogy/family.revision/easy-family-member.yaml"
    FamilyFilter:
      $ref: "./model/genealogy/family.revision/family-filter.yaml"
    FamilyMemberFilter:
      $ref: "./model/genealogy/family.revision/family-member-filter.yaml"
    FamilyMemberFullInfo:
      properties:
        familyMember:
          $ref: "#/components/schemas/EasyFamilyMember"
        anotherFamilies:
          description: Список архивов с вложенным списком семей
          type: array
          items:
            $ref: "./model/genealogy/family.revision/archive-with-family-members.yaml"
    ErrorResponse:
      $ref: "./model/common/error-response.yaml"
